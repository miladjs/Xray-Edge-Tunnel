# Xray-Edge-Tunnel | نصب کننده خودکار Xray + CDN + SSL

[![English](https://img.shields.io/badge/Language-English-blue?style=flat-square)](README_EN.md)

> [!WARNING]
> **نکته مهم در مورد اتصال:**
> در اولین اتصال ممکن است حدود ۶ تا ۷ ثانیه طول بکشد تا کلادفلر ارتباط با CDN را برقرار کند و خطا برطرف شود. لطفاً در اولین تلاش کمی صبور باشید تا اتصال پایدار شود.

> [!CAUTION]
> **نکات بسیار مهم قبل از شروع:**
> 1. **دامین غیر IR**: حتماً از دامین‌های بین‌المللی (مانند .com, .org, .net, .site و ...) استفاده کنید. دامین‌های **.ir** روی کلادفلر پشتیبانی نمی‌شوند.
> 2. **تنظیمات پروکسی**: در ابتدای نصب و برای دریافت گواهی SSL، پروکسی کلادفلر (ابر) باید **خاموش (Grey Cloud)** باشد. تنها **پس از اتمام نصب** می‌توانید آن را روشن (Orange Cloud) کنید.

این اسکریپت نصب و پیکربندی **Xray (VLESS-WebSocket-TLS)** را با پشتیبانی از **Cloudflare CDN** به صورت خودکار انجام می‌دهد. این ابزار یک پروکسی سرور امن و سریع را برای عبور از فیلترینگ با استفاده از قابلیت‌های CDN راه‌اندازی می‌کند.

## 🚀 آپدیت بزرگ (مدیریت کامل)

> **تغییرات نسخه جدید (Full Manager):**
> - **منوی مدیریت کامل**: اضافه شدن منوی تعاملی برای مدیریت راحت‌تر (نصب، حذف، لاگ و ...).
> - **پشتیبانی از پروتکل xhttp**: جدیدترین و پایدارترین پروتکل برای عبور از محدودیت‌ها.
> - **مدیریت کاربران**: قابلیت افزودن، حذف و مشاهده لیست کاربران با UUID اختصاصی.
> - **تنظیمات پیشرفته**: امکان انتخاب **ALPN** (h2/http1.1) و **Fingerprint** (Chrome, Firefox, Safari).
> - **مدیریت سرویس**: استارت، استاپ، ریستارت و مشاهده وضعیت سرویس از داخل اسکریپت.
> - **رفع مشکلات**: حل مشکل دسترسی فایل‌های SSL (اجرا با کاربر root).

## ✨ ویژگی‌ها

- **نصب و مدیریت خودکار**: نصب Xray، Certbot و مدیریت کامل سرویس از طریق منو.
- **گواهی SSL رایگان**: دریافت خودکار گواهی SSL معتبر از Let's Encrypt با قابلیت Fallback.
- **پروتکل‌های متنوع**: پشتیبانی از **xhttp** (پیشنهادی) و **WebSocket** قدیمی.
- **امنیت بالا**: تنظیمات پیشرفته TLS، Fingerprint و ALPN.
- **مدیریت کاربران**: سیستم مدیریت کاربران چندگانه با لینک‌های اختصاصی.
- **نصب تعاملی و هوشمند**:
    - **بررسی DNS**: بررسی خودکار اتصال دامین به IP سرور (IPv4 و IPv6).
    - **بررسی سیستم**: بررسی سازگاری سیستم عامل و اتصال اینترنت.

## 📋 پیش‌نیازها

- یک **سرور مجازی (VPS)** با سیستم عامل **Ubuntu 20.04+** یا **Debian 10+**.
- یک **دامین** که از طریق Cloudflare به سرور شما متصل شده باشد.
    - **رکورد DNS**: یک رکورد `A` (برای IPv4) یا `AAAA` (برای IPv6) ایجاد کنید که به IP سرور شما اشاره کند.
    - **وضعیت پروکسی**: برای دریافت SSL، ابتدا باید روی **DNS Only (ابر خاکستری)** باشد. پس از اتمام نصب، می‌توانید آن را به **Proxied (ابر نارنجی)** تغییر دهید.
- **پورت 80**: باید برای تاییدیه Let's Encrypt باز باشد.

## 🚀 نصب

دستور زیر را در سرور خود (با دسترسی root) اجرا کنید:

```bash
bash <(curl -Ls https://raw.githubusercontent.com/miladjs/Xray-Edge-Tunnel/main/xray-cdn-installer.sh)
```

## 🛠️ راهنمای استفاده

1.  **اجرای اسکریپت**: دستور نصب را اجرا کنید.
2.  **وارد کردن دامین**: نام دامین خود را وارد کنید (مثال: `example.com`).
3.  **بررسی DNS**: اسکریپت IPهای سرور را شناسایی کرده و راهنمای تنظیم DNS را نمایش می‌دهد.
    - می‌توانید انتخاب کنید که قبل از ادامه، صحت رکوردهای DNS به صورت خودکار بررسی شود.
4.  **تنظیمات سفارشی**:
    - **مسیر WebSocket**: مسیر دلخواه را وارد کنید یا اینتر بزنید تا پیش‌فرض (`/graphql`) انتخاب شود.
    - **هاست CDN**: هاست CDN را وارد کنید یا اینتر بزنید تا پیش‌فرض (`chatgpt.com`) انتخاب شود.
    - **پورت**: پورت مورد نظر را وارد کنید یا اینتر بزنید تا پیش‌فرض (`443`) انتخاب شود. اسکریپت آزاد بودن پورت را چک می‌کند.
5.  **صبر کنید**: اسکریپت Xray را نصب کرده، SSL می‌گیرد و کانفیگ را می‌سازد.
6.  **دریافت لینک**: لینک `vless://` تولید شده را کپی کرده و در کلاینت خود ایمپورت کنید.
7.  **فعال‌سازی CDN**: به پنل Cloudflare بروید و پروکسی (ابر نارنجی) را برای دامین خود روشن کنید.

## 📱 کلاینت‌های پیشنهادی

-   **ویندوز**: [v2rayN](https://github.com/2dust/v2rayN)
-   **اندروید**: [v2rayNG](https://github.com/2dust/v2rayNG)
-   **آی‌او‌اس (iOS)**: [V2Box](https://apps.apple.com/us/app/v2box-v2ray-client/id6446814690) یا [Shadowrocket](https://apps.apple.com/us/app/shadowrocket/id932747118)
-   **مک (macOS)**: [V2rayU](https://github.com/yanue/V2rayU) یا [Foxray](https://apps.apple.com/us/app/foxray/id6448898396)

## ⚠️ نکات مهم

-   **محل فایل‌ها**:
    -   فایل کانفیگ: `/usr/local/etc/xray/config.json`
    -   لاگ نصب: `/var/log/xray-installer.log`
    -   اطلاعات کانفیگ کاربر: `/root/xray-config.txt`
-   **تنظیمات Cloudflare**: مطمئن شوید که حالت SSL/TLS در کلادفلر روی **Full** یا **Full (Strict)** تنظیم شده باشد.
-   **تمدید خودکار**: گواهی SSL به صورت خودکار تمدید می‌شود و نیاز به اقدام دستی نیست.

## 🤝 مشارکت

از مشارکت‌ها، گزارش باگ‌ها و درخواست ویژگی‌های جدید استقبال می‌کنیم! لطفاً به [صفحه Issues](https://github.com/miladjs/Xray-Edge-Tunnel/issues) مراجعه کنید.

## 📄 لایسنس

این پروژه تحت لایسنس MIT منتشر شده است.
